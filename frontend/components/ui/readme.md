# UI Components

Contains basic, reusable, atomic UI primitives like buttons, inputs, modals, cards, badges, tooltips, etc.
These components should follow a strict design system, be styled using Vanilla CSS modules or variables, and not contain business logic.
